# **PlayPro API - وثائق واجهة برمجة التطبيقات (API Documentation)**

## **🎯 نظرة عامة**
**PlayPro App** هو نظام إداري متكامل مصمم خصيصاً لإدارة الملاعب الرياضية، الأكاديميات، الصالات، والعمليات المتعلقة بها. يوفر النظام حلاً شاملاً يغطي إدارة الحجوزات، المستخدمين، المدفوعات، المحتوى، والعمليات الإدارية.

### **🔗 الأساسيات**
- **Base URL:** `https://playpro.abdaealmasi.store/api`
- **Schema:** RESTful API
- **Authentication:** Bearer Token (JWT)
- **Response Format:** JSON

---

## **📊 فهرس واجهات API**

### **🔐 التوثيق والمصادقة (Auth)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| POST | `/register` | تسجيل مستخدم جديد | عام |
| POST | `/registerOwner` | تسجيل مالك أو مدرب | عام |
| POST | `/login` | تسجيل الدخول | عام |
| POST | `/users/{id}/role` | تحديث دور المستخدم | Admin |
| GET | `/pending-registrations` | الحصول على التسجيلات المعلقة | Admin |
| POST | `/admin/users/create` | إنشاء مستخدم جديد | Admin |
| POST | `/forgot-password` | طلب إعادة تعيين كلمة المرور | عام |
| POST | `/reset-password-otp` | تأكيد OTP وإعادة تعيين كلمة المرور | عام |
| POST | `/users/logout` | تسجيل الخروج | أي مستخدم مسجل |

### **👤 إدارة المستخدمين (Users)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/users/profile` | عرض الملف الشخصي | أي مستخدم مسجل |
| POST | `/users/profile` | تحديث الملف الشخصي | أي مستخدم مسجل |
| POST | `/users/reset-password` | تغيير كلمة المرور | أي مستخدم مسجل |
| GET | `/api/users` | عرض جميع المستخدمين | Admin |
| DELETE | `/users/{id}` | حذف مستخدم | Admin |

### **⚽ إدارة الملاعب (Fields)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/fields` | عرض جميع الملاعب | Owner, Admin |
| POST | `/fields` | إنشاء ملعب جديد | Owner, Admin |
| PUT | `/fields/{id}` | تحديث بيانات الملعب | Owner, Admin |
| GET | `/my-fields` | عرض الملاعب الخاصة بي | Owner |
| GET | `/fields/{id}/periods` | عرض فترات الملعب | أي مستخدم مسجل |
| POST | `/fields/{id}/periods` | إضافة فترة جديدة | Owner, Admin |
| GET | `/fields/{id}/images` | عرض صور الملعب | أي مستخدم مسجل |
| POST | `/fields/{id}/images` | رفع صور للملعب | Owner, Admin |
| DELETE | `/field-images/{id}` | حذف صورة ملعب | Owner, Admin |

### **📅 إدارة الحجوزات (Bookings)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| POST | `/bookField` | حجز ملعب (حجز سريع) | أي مستخدم مسجل |
| POST | `/bookings` | إنشاء حجز جديد | أي مستخدم مسجل |
| POST | `/bookings/contract` | إنشاء حجز تعاقدي | OwnerAcademy, Admin |
| GET | `/my-bookings` | عرض حجوزاتي | أي مستخدم مسجل |
| GET | `/api/bookings` | عرض جميع الحجوزات | Admin, Owner |
| POST | `/bookings/{id}/cancel` | إلغاء الحجز | مالك الحجز، Admin |
| POST | `/bookings/verify-qr` | التحقق من رمز QR | Owner, Admin |
| POST | `/bookings/{id}/renew` | تجديد الحجز | مالك الحجز، Admin |
| POST | `/admin/bookings/{id}` | تحديث بيانات الحجز | Admin |
| GET | `/bookings/my` | عرض حجوزاتي (بديل) | أي مستخدم مسجل |

### **🔄 طلبات النقل (Transfer Requests)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| POST | `/transfer-requests` | إنشاء طلب نقل | أي مستخدم مسجل |
| GET | `/transfer-requests` | عرض طلبات النقل | Admin, Owner, Coach |
| POST | `/transfer-requests/{id}/approve` | الموافقة على طلب النقل | Admin, Owner, Coach |
| DELETE | `/transfer-requests/{id}` | حذف طلب نقل | Admin, Creator |

### **👨‍🏫 إدارة المدربين (Coaches)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/coaches` | عرض جميع المدربين | أي مستخدم |
| POST | `/api/coaches` | إضافة مدرب جديد | Owner, Admin |
| GET | `/coaches/{id}` | عرض بيانات مدرب | أي مستخدم |
| POST | `/api/coaches/{id}` | تحديث بيانات مدرب | Owner, Admin |
| DELETE | `/coaches/{id}` | حذف مدرب | Owner, Admin |

### **🎓 إدارة الأكاديميات (Academies)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/academies` | عرض جميع الأكاديميات | أي مستخدم |
| GET | `/academies/{id}` | عرض أكاديمية محددة | أي مستخدم |
| POST | `/academies` | إنشاء أكاديمية جديدة | OwnerAcademy, Admin |
| POST | `/academies/{id}` | تحديث بيانات الأكاديمية | OwnerAcademy, Admin |
| DELETE | `/academies/{id}` | حذف أكاديمية | OwnerAcademy, Admin |
| GET | `/academies/academies/me` | عرض أكاديمياتي | OwnerAcademy |

### **🛒 إدارة المنتجات والطلبات (Products & Orders)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/products` | عرض جميع المنتجات | أي مستخدم |
| POST | `/api/products` | إضافة منتج جديد | Admin |
| GET | `/api/products/{id}` | عرض منتج محدد | أي مستخدم |
| POST | `/api/products/{id}` | تحديث منتج | Admin |
| DELETE | `/api/products/{id}` | حذف منتج | Admin |
| GET | `/api/orders` | عرض جميع الطلبات | Admin |
| POST | `/api/orders` | إنشاء طلب جديد | أي مستخدم مسجل |
| GET | `/api/orders/{id}` | عرض طلب محدد | مالك الطلب، Admin |
| DELETE | `/api/orders/{id}` | حذف طلب | Admin |

### **🎫 إدارة العروض والكوبونات (Coupons)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/coupons` | عرض جميع الكوبونات | Admin |
| POST | `/api/coupons` | إنشاء كوبون جديد | Admin |
| GET | `/api/coupons/{id}` | عرض كوبون محدد | Admin |
| PUT | `/api/coupons/{id}` | تحديث كوبون | Admin |
| DELETE | `/api/coupons/{id}` | حذف كوبون | Admin |
| POST | `/api/coupons/check` | التحقق من صحة الكوبون | أي مستخدم مسجل |

### **⭐ نظام الولاء والتقييمات (Loyalty & Ratings)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/loyalty-points` | عرض نظام النقاط | Admin |
| POST | `/loyalty-points` | إضافة مستوى نقاط جديد | Admin |
| PUT | `/loyalty-points/{id}` | تحديث مستوى نقاط | Admin |
| DELETE | `/loyalty-points/{id}` | حذف مستوى نقاط | Admin |
| POST | `/api/rate` | إنشاء/تحديث تقييم | أي مستخدم مسجل |

### **📢 إدارة المحتوى (Content Management)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/news-events` | عرض الأخبار والفعاليات | أي مستخدم |
| POST | `/api/news-events` | إضافة خبر/فعالية | Admin |
| POST | `/api/news-events/{id}` | تحديث خبر/فعالية | Admin |
| DELETE | `/api/news-events/{id}` | حذف خبر/فعالية | Admin |
| GET | `/api/blogs` | عرض المدونات | أي مستخدم |
| POST | `/api/blogs` | إضافة مدونة | Admin |
| POST | `/api/blogs/{id}` | تحديث مدونة | Admin |
| DELETE | `/api/blogs/{id}` | حذف مدونة | Admin |
| GET | `/api/banners` | عرض البانرات | أي مستخدم |
| POST | `/api/banners` | إضافة بانر | Admin |
| POST | `/api/banners/{id}` | تحديث بانر | Admin |
| DELETE | `/api/banners/{id}` | حذف بانر | Admin |

### **🏢 معلومات النظام والتواصل (System & Contact)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/app-info` | عرض معلومات التطبيق | أي مستخدم |
| POST | `/api/app-info` | تحديث معلومات التطبيق | Admin |
| POST | `/api/contact-us` | إرسال رسالة تواصل | عام |
| GET | `/api/contact-messages` | عرض رسائل التواصل | Admin |
| GET | `/api/contact-messages/{id}` | عرض رسالة محددة | Admin |
| GET | `/api/partners` | عرض الشركاء | أي مستخدم |
| POST | `/api/partners` | إضافة شريك | Admin |
| POST | `/api/partners/{id}` | تحديث شريك | Admin |
| DELETE | `/api/partners/{id}` | حذف شريك | Admin |

### **📄 الشروط والبنود (Terms & About)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| GET | `/api/terms` | عرض الشروط والأحكام | أي مستخدم |
| POST | `/api/terms` | إضافة بند جديد | Admin |
| PUT | `/api/terms/{id}` | تحديث بند | Admin |
| DELETE | `/api/terms/{id}` | حذف بند | Admin |
| GET | `/api/about` | عرض معلومات عنا | أي مستخدم |
| POST | `/api/about` | إضافة قسم جديد | Admin |
| POST | `/api/about/{id}/upload-image` | رفع صورة للقسم | Admin |
| PUT | `/api/about/{id}` | تحديث قسم | Admin |
| DELETE | `/api/about/{id}` | حذف قسم | Admin |

### **📞 خدمات التواصل (Communication Services)**
| الطريقة | المسار | الوصف | الصلاحيات |
|---------|--------|--------|-----------|
| POST | `/api/whatsapp/send` | إرسال رسالة واتساب | Admin |

---

## **🔑 نظام الصلاحيات**

### **👥 أدوار المستخدمين**
1. **المستخدم العادي (User)**
   - التسجيل والدخول
   - حجز الملاعب
   - عرض حجوزاته
   - التقييم

2. **مالك الملعب (Owner)**
   - جميع صلاحيات المستخدم العادي
   - إدارة ملاعبه الخاصة
   - إضافة وتعديل الفترات
   - إدارة الحجوزات في ملاعبه
   - إضافة مدربين

3. **مالك الأكاديمية (OwnerAcademy)**
   - جميع صلاحيات المالك
   - إدارة الأكاديميات
   - إنشاء الحجوزات التعاقدية

4. **المدرب (Coach)**
   - عرض الحجوزات في الملاعب المعين عليها
   - إدارة طلبات النقل

5. **المسؤول (Admin)**
   - جميع الصلاحيات
   - إدارة النظام بالكامل
   - إدارة جميع المستخدمين
   - إدارة المحتوى
   - الإحصائيات والتقارير

---

## **📱 حالات الاستخدام النموذجية**

### **🎯 للمستخدم العادي**
1. **التسجيل وتسجيل الدخول** (`/register`, `/login`)
2. **تصفح الملاعب المتاحة** (`/fields`)
3. **حجز ملعب** (`/bookField` أو `/bookings`)
4. **عرض حجوزاته** (`/my-bookings`)
5. **تعديل الملف الشخصي** (`/users/profile`)
6. **التقييم** (`/api/rate`)

### **👑 لمالك الملعب**
1. **إضافة ملعب جديد** (`/fields`)
2. **إضافة فترات للملعب** (`/fields/{id}/periods`)
3. **عرض حجوزات ملاعبه** (`/api/bookings`)
4. **إدارة المدربين** (`/api/coaches`)
5. **الموافقة على طلبات النقل** (`/transfer-requests/{id}/approve`)

### **🎓 لمالك الأكاديمية**
1. **إنشاء أكاديمية** (`/academies`)
2. **إنشاء حجز تعاقدي** (`/bookings/contract`)
3. **إدارة الاشتراكات والتجديدات** (`/subscription-renewal-prices`)

### **👨‍💼 للمسؤول**
1. **إدارة جميع المستخدمين** (`/api/users`)
2. **الموافقة على التسجيلات المعلقة** (`/pending-registrations`)
3. **إدارة المحتوى** (`/news-events`, `/blogs`, `/banners`)
4. **إدارة الكوبونات والعروض** (`/api/coupons`)
5. **مراقبة النظام والإحصائيات** (جميع الـ APIs)

---

## **⚙️ نماذج البيانات الرئيسية**

### **📋 نموذج تسجيل المستخدم**
```json
{
  "name": "اسم المستخدم",
  "email": "البريد الإلكتروني",
  "phone": "رقم الهاتف",
  "password": "كلمة المرور"
}
```

### **⚽ نموذج إنشاء ملعب**
```json
{
  "name": "اسم الملعب",
  "size": "المقاس (20x40)",
  "capacity": 10,
  "latitude": 30.0444,
  "longitude": 31.2357,
  "city": "المدينة",
  "address": "العنوان",
  "description": "الوصف",
  "periods": [
    {
      "start_time": "09:00",
      "end_time": "11:00",
      "price_per_player": 50
    }
  ]
}
```

### **📅 نموذج حجز ملعب**
```json
{
  "field_id": 22,
  "period_id": 197,
  "name": "اسم الحاجز",
  "phone": "رقم الهاتف",
  "date": "2026-01-10",
  "payment_method": "cash",
  "payment_type": "deposit"
}
```

### **📝 نموذج حجز تعاقدي**
```json
{
  "user_name": "اسم المستخدم",
  "user_email": "البريد الإلكتروني",
  "user_password": "كلمة المرور",
  "user_phone": "رقم الهاتف",
  "field_id": 23,
  "days": ["Saturday", "Tuesday", "Thursday"],
  "start_date": "2026-02-01",
  "end_date": "2026-08-01",
  "start_time": "16:00",
  "end_time": "18:00",
  "contract_price": 12000,
  "payment_method": "credit",
  "paid": 5000
}
```

---

## **🔒 نقاط مهمة للأمان**

### **🔑 إدارة التوكنات**
- كل توكن له صلاحية محددة حسب دور المستخدم
- التوكن ينتهي بعد فترة زمنية محددة
- يمكن تسجيل الخروج وإبطال التوكن (`/users/logout`)

### **👮 التحكم في الصلاحيات**
- التحقق من الصلاحيات قبل تنفيذ أي عملية
- لا يمكن للمستخدم العادي الوصول لبيانات مستخدمين آخرين
- المالك يتحكم فقط في ملاعبه وليس كل الملاعب

### **🔐 حماية البيانات**
- كلمات المرور مشفرة
- البيانات الحساسة لا تُعاد في الـ Response
- التحقق من صحة المدخلات

---

## **🚀 أفضل الممارسات**

### **✅ عند التطوير**
1. استخدم متغيرات البيئة للـ Base URL
2. احفظ التوكنات في مكان آمن
3. تأكد من تحديث التوكن قبل انتهاء صلاحيته
4. تحقق من صحة الـ Response دائماً

### **📊 عند الاختبار**
1. ابدأ باختبار APIs المصادقة أولاً
2. اختبر كل دور مستخدم بشكل منفصل
3. تأكد من التحكم في الصلاحيات
4. اختبر الحالات الحدية والأخطاء

### **🔧 عند التكامل**
1. استخدم الـ Headers المناسبة
2. تعامل مع الـ Errors بشكل صحيح
3. نفذ نظام Retry للعمليات الفاشلة
4. سجل الـ Logs للعمليات المهمة

---

## **📞 الدعم والمساعدة**

### **🛠️ استكشاف الأخطاء**
1. **خطأ 401**: التوكن غير صالح أو منتهي الصلاحية
2. **خطأ 403**: ليس لديك صلاحية لهذا الإجراء
3. **خطأ 404**: المسار غير موجود
4. **خطأ 422**: بيانات الإدخال غير صحيحة

### **📋 نصائح للتصحيح**
1. تحقق من صحة التوكن والمصادقة
2. تأكد من أنك تستخدم الـ HTTP Method الصحيح
3. تحقق من الـ Headers المطلوبة
4. تأكد من صحة تنسيق البيانات (JSON/FormData)

---

## **📈 خطط التطوير المستقبلية**

### **🔄 تحسينات قادمة**
1. **نظام الدفع الإلكتروني** المتكامل
2. **التقارير والإحصائيات** المتقدمة
3. **نظام الإشعارات** الفورية
4. **واجهة تحكم شاملة** للمسؤولين
5. **التكامل مع خرائط جوجل** للعثور على الملاعب

---

**✨ تم تطوير PlayPro App ليكون الحل الشامل لإدارة الملاعب والأكاديميات الرياضية، مع التركيز على البساطة، الكفاءة، والأمان.**
